#include "main.h"
/**
*mul- function that multiplies two integers.
*@a: tested character
*@b: tested character
*Return: 0
*/
int mul(int a, int b)
{
int i, x, result;
result = i * x ;
putchar(result, '0');
return (0);
}
